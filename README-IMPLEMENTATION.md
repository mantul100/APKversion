# Backend scaffold for JagoPOS (minimal)

A minimal Express + Postgres scaffold with core endpoints for checkout, sync, and payment webhook. Intended as a starting point — extend for production (validation, auth, RBAC, logging, migrations tooling).

Run with Docker Compose (recommended) or Node directly.
